tags:
  # Validacao mutavel controlada contra o GLPI real (conta de teste dedicada).
  # Skipada por padrao quando faltam credenciais; muta so com SIS_VALIDATION_APPLY=true.
  # Rodar isolada: flutter test --tags mutable-validation
  mutable-validation:
